{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "description": "PatchPal MCP Server Configuration - copy to ~/.patchpal/config.json",
  "mcp": {
    "demo": {
      "type": "local",
      "command": ["python", "examples/mcp/simple_server.py"],
      "enabled": true,
      "description": "Example local MCP server with basic tools"
    },
    "huggingface": {
      "type": "remote",
      "url": "https://huggingface.co/mcp",
      "enabled": false,
      "headers": {
        "Authorization": "Bearer ${HF_TOKEN}"
      },
      "description": "Hugging Face Hub - search models, datasets, Spaces, papers. Get token: https://huggingface.co/settings/tokens"
    },
    "filesystem": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/directory"],
      "enabled": false,
      "description": "Secure file operations with access controls. Change path to your allowed directory."
    },
    "memory": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-memory"],
      "enabled": false,
      "description": "Knowledge graph-based persistent memory across sessions"
    },
    "fetch": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-fetch"],
      "enabled": false,
      "description": "Fetch and convert web content to markdown"
    },
    "github": {
      "type": "remote",
      "url": "https://api.github.com/mcp",
      "enabled": false,
      "headers": {
        "Authorization": "Bearer ${GITHUB_TOKEN}"
      },
      "description": "GitHub API access. Get token: https://github.com/settings/tokens"
    },
    "example_with_env_vars": {
      "type": "local",
      "command": ["your-mcp-server-command"],
      "enabled": false,
      "environment": {
        "API_KEY": "${YOUR_API_KEY}",
        "DB_HOST": "${DATABASE_HOST:-localhost}",
        "DB_PASSWORD": "${DATABASE_PASSWORD}"
      },
      "description": "Example showing environment variable expansion: ${VAR} or ${VAR:-default}"
    }
  }
}
